node_modules/
dist/
.output/
.tanstack/
.vercel/
.vite/
.pytest_cache/
__pycache__/
*.pyc
*.tsbuildinfo
.env
.env.local
.grok/
.project_id
.node_modules.lock

artifacts/
attachments/
screenshots/
browsed_files/

# Terrain high-poly trop lourd pour git (>50 Mo)
public/models/snow-mountain-hi.glb

# Chrome PWA Grok (preview sandbox)
public/__grok/

# TanStack Router le réécrit à chaque `npm run dev` (surtout sous Windows).
# Il n'a rien à faire dans Git : sinon chaque pull est bloqué.
src/routeTree.gen.ts

# Contrat sandbox, pas le produit
AGENTS.md
AGENTS.project.md
.claude/
.skitrack-url
openapi.json
out/
scrape/**/tools/
scrape/**/.browsers/
sidecar/
tools/
vendor/

release/

# Tuiles OpenSkiMap : 135 Mo, bâties par scripts/build-tuiles-openskimap.ts,
# hébergées hors du dépôt (voir docs/TUILES-OPENSKIMAP.md).
public/carte/*.pmtiles

# Venv des workers Python de relevé (npm run scrape:python).
scrape/.venv/
