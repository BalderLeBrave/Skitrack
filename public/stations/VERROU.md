# VERROUILLÉ — images

Écriture interdite sans accord explicite du propriétaire, dans le même message :

- `j'autorise la modification des images`

Pas de téléchargement, recadrage, renommage ni `--write` sur
`scripts/fetch-missing-photos.mjs`.

Règle complète : `CLAUDE.md` à la racine du dépôt.
